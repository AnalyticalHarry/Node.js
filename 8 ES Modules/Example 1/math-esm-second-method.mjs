// arithmetic operations 
export default {
    power: (a, b) => a ** b,
    sqrt: (a) => Math.sqrt(a),
    log: (a) => Math.log(a),
    exp: (a) => Math.exp(a),
    abs: (a) => Math.abs(a),
    cube: (a) => Math.pow(a, 3)
};
